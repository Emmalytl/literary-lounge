// Placeholder types. Once your Supabase project is running the migrations
// in supabase/migrations/, generate real types with:
//
//   npx supabase gen types typescript --project-id <your-project-ref> > src/lib/database.types.ts
//
// Using `any` here keeps the app compiling before that step; replace it
// as soon as you have real generated types so you get full type safety.
export type Database = any
